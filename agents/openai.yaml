interface:
  display_name: "Commit Roast"
  short_description: "Roast a diff, then make it commit-ready."
  default_prompt: "Use $commit-roast to review my staged diff, roast the risky parts, and suggest a commit message."

policy:
  allow_implicit_invocation: true
